apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: gitea-ingress
  namespace: gitea
spec:
  ingressClassName: nginx
  rules:
  - host: tcl.gitea.com
    http:
      paths:
      - backend:
          service:
            name: gitea
            port:
              number: 3000
        path: /
        pathType: Prefix
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: amcop-sys-ingress
  namespace: amcop-system
spec:
  ingressClassName: nginx
  rules:
  - host: tcl.app.com
    http:
      paths:
      - backend:
          service:
            name: auth-gateway
            port:
              number: 5000
        path: /
        pathType: Prefix